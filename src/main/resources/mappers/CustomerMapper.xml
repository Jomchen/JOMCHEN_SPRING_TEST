<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jomchen.springtest.mappers.CustomerMapper">

    <select id="get" resultType="com.jomchen.springtest.entity.basedata.Customer">
        SELECT * FROM customer WHERE cid = #{cid}
    </select>

</mapper>